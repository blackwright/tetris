var TETRIS = TETRIS || {};

TETRIS.Model = ( function(TetMaker) {
  'use strict';

  let board, tetromino;
  let score = 0;

  let init = () => {

  };

  return {
    init: init
  };

})(TETRIS.TetMaker);
